#include <stdio.h>
#include <stdlib.h>
#include <math.h>

struct ponto {
	float x;
	float y;
} typedef struct ponto Ponto;

void preencher(Ponto x, y) {
	int x, y;
	x = 3;
	y = 10
}

void distancia(int x,) {
	
}

int main () {
	float d;
	Ponto *P1, *P2;
	
	P1 = (Ponto*)malloc(sizeof(Coord));
	P2 = (Ponto*)malloc(sizeof(Coord));
	
	preencher(P1);
	preencher(P2);
	
	d = distancia(P1, P2);
}